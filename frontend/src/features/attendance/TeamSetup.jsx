/**
 * Team Setup — Client Focal Email(s) live next to Supervisor allocation
 * inside AttendanceManagement.jsx (TeamAdmin).
 * This path is the MD plan alias; the live UI is mounted via Attendance Management tabs.
 */
export { default } from '../../AttendanceManagement.jsx';
