# S9 — Post-milestone backlog (sequenced; each item gets its own mini-plan before execution)

> These are NOT session instructions yet. When a backlog item is scheduled, write a dedicated session file for it following the house format (rules header, objective, steps, verification, rollback) and the then-current state of the code. Order below is the recommended sequence; the MD may reorder by business need.

1. **PSO deliverable-based billing.** Manhour/billing computation from deliverable actuals (cans/drums): new `deliverable_actuals` table (migration) keyed to `contract_bid_items`; monthly entry UI; `applyBillingAmount`/invoice generation honors deliverable-derived billing for contracts flagged for it. Salaries already flow via the normal engine — this is invoice-side only.
2. **Medical plan tiers.** Replace the single `medical_annual_cap` with structured plan tiers (self/spouse/children coverage levels per contract), enforcement in claims approval (cap checks against `benefit_utilization`), and payslip visibility.
3. **Frontend restructure** (only after all contracts stable on runs): react-router (URL-addressable tabs, refresh-safe), TanStack Query for server state, eliminate the 63 raw `fetch()` calls and 5 private fetch wrappers (all through `api.js`), consolidate InvoiceSection vs features/ar (retire legacy `invoices`-table paths in the UI), ESLint burn-down from 261 errors (start with the 144 static-components), split the 1,000+ line components as they're touched — never big-bang.
4. **server.js decomposition.** Route-group-by-route-group into `backend/src/modules/*`, each extraction gated on green tests-int for that group; after the last group: physically remove the inline CREATE TABLE block (schema bootstrap becomes schema.sql + migrations only — verified continuously by the tests-int bootstrap since S3).
5. **Ops upgrade.** $7 Render background worker running `worker.js` (`JOBS_RUNNER=worker`); move pg-boss crons out of the web dyno; then activate the email intake hub with real GMAIL_*/INTAKE creds and monitor `intake_messages` for a soak period before trusting auto-routing.
6. **Legacy `invoices` table → read-only** (410 the write routes, keep reads), completing the client_invoices consolidation.
7. **Portal expansion.** After portal-readiness data quality is fixed (S1C report drives an HR data cleanup): payslip access, leave requests with contract-aware policies (the leave module's optional contract parameter is already plumbed but unused — wire it), claims submission as the default channel.
